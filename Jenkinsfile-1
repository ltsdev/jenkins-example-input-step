pipeline {
  agent any
  stages {
    stage('input') {
      steps {
        input(message: 'Hello World!', ok: 'Submit')
        sh '''
          hostname
          cat /etc/os-release
          echo ${env.GIT_TAG_NAME}
        '''
      }
    }
  }
}
